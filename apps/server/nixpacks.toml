[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["cd ../.. && bun install"]

[phases.build]
cmds = ["cd ../.. && bun run build --filter=server..."]

[start]
cmd = "cd apps/server && bun run src/index.ts"

[variables]
PORT = "8080"